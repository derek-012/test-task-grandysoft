//import Collision from "./collision.js";

class Line {
	constructor(begin, end) {
		this.begin = begin;
		this.end = end;
	}

	// cross(line) {
	// 	Collision.getCollision(this, line);
	// } 
}